_schema-version: "3.2"
ID: zinvoices
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: zinvoices-destination-content
  type: com.sap.application.content
  requires:
  - name: zinvoices-destination-service
    parameters:
      content-target: true
  - name: zinvoices-repo-host
    parameters:
      service-key:
        name: zinvoices-repo-host-key
  - name: zinvoices-uaa
    parameters:
      service-key:
        name: zinvoices-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: zinvoices_html_repo_host
          ServiceInstanceName: zinvoices-html5-service
          ServiceKeyName: zinvoices-repo-host-key
          sap.cloud.service: zinvoices
        - Authentication: OAuth2UserTokenExchange
          Name: zinvoices_uaa
          ServiceInstanceName: zinvoices-xsuaa-service
          ServiceKeyName: zinvoices-uaa-key
          sap.cloud.service: zinvoices
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: zinvoices-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: zinvoices-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - zinvoices.zip
      name: zinvoices
      target-path: resources/
- name: zinvoices
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: zinvoices-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: zinvoices-destination-service
    service-plan: lite
- name: zinvoices-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: zinvoices-xsuaa-service
    service-plan: application
- name: zinvoices-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: zinvoices-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
